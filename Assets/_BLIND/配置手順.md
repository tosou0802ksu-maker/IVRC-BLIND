# BLIND ギミック配置手順（ワールド班向け）

計画書(Excalidraw)の仕様に対応するスクリプトは全部書いてある。
シーンへの配置はこの手順どおりにInspectorを繋ぐだけで動く。

---

## 0. レイヤー（設定済み）

| index | 名前 | 用途 |
|---|---|---|
| 22 | Thermal | サーモ役だけに見えるもの（熱源・落とし穴の警告・蛍光灯） |
| 23 | Echo | エコロケ役だけに見えるもの（感圧板・普通の穴） |
| 24 | Memory | 過去の人だけに見えるもの（看板・矢印・クイズの文字・偽ボタンの色） |

**全員に見せたい壁・床はDefault(0)のまま。** 役割専用のものだけ専用レイヤーに移す。

---

## 1. PlayerVisionController（役割別の見え方）

空のGameObjectを1つ作ってアタッチ。カリングマスクは以下の数値になるようチェックを入れる。

| フィールド | チェックするレイヤー |
|---|---|
| echoCullingMask | Default, Echo |
| thermalCullingMask | Default, Thermal |
| memoryCullingMask | Default, Memory, Player, PlayerLocal |

Echo/Thermalで **Player を外している** のが重要。他人の実アバターが消えて、代わりに
`RemotePlayerProxyManager` が出す球体／熱源が見える、という作りになっている。

※ VRChat実機ではシーンのCameraではなく `VRCCameraSettings.ScreenCamera` を書き換える実装に変更済み。
　 `localCamera` はエディタ確認用なので未設定でよい。

---

## 1.5. RemotePlayerProxyManager + HumanoidProxyRig（他プレイヤーの見え方）

PlayerVisionControllerでPlayerレイヤーを外しただけだと、Echo/Thermal役には**他プレイヤーが何も見えなくなる**。
代わりに主要な関節を追従する「人型プロキシ」を表示させることで、
「エコロケなら人型のシルエット」「サーマルなら人型の熱源」として相手の姿・ポーズが分かるようにする
(球体1個が浮いているだけではなく、しゃがむ/手を伸ばす等のポーズも再現される)。

### A. HumanoidProxyRigプレハブを1個作る(最初に1回だけ)

カプセル(Unity標準のCapsule)を10本、球体を1個使って人型を組む。

| Transform名(例) | 対応する関節 | 形 |
|---|---|---|
| Torso | Hips-Chest | カプセル |
| Neck | Chest-Head | カプセル |
| Head | Head | 球体 |
| LeftUpperArm / LeftForearm | 左肩-左肘-左手 | カプセル×2 |
| RightUpperArm / RightForearm | 右肩-右肘-右手 | カプセル×2 |
| LeftThigh / LeftShin | 左腰-左膝-左足 | カプセル×2 |
| RightThigh / RightShin | 右腰-右膝-右足 | カプセル×2 |

各カプセル/球体は**そのままだと太すぎる**ので、最初に置いた時点で `localScale.x / z` を細め(0.15〜0.3くらい)に調整しておく。
**`localScale.y` はスクリプトが毎フレーム自動で書き換えて伸縮させるので、初期値のままでいい。**
位置・回転もスクリプトが上書きするので合わせなくてよい。

各パーツ(11個)それぞれの中に、**同じ大きさ・同じ位置で重ねて**以下の2つの子メッシュを入れる。

- **輪郭用メッシュ** → レイヤーを **Echo(23)** に設定(色は単色のシルエットでOK)
- **熱表現用メッシュ**(`ThermalHeat.shader` を使ったマテリアル) → レイヤーを **Thermal(22)** に設定

これで11パーツ×2メッシュ = 22個の子メッシュができる。できたらこの11パーツをまとめて1つの親にし、`HumanoidProxyRig` をアタッチして、Inspectorの `torso` `neck` `head` `leftUpperArm`… の各フィールドに対応するTransformを入れる。
`thermalRenderers` には、22個のうちThermalレイヤー側のメッシュのRendererを**全部**入れる(熱の強さを一括制御するため)。

できたら **Prefab化** しておく(2体分複製して使うため)。

### B. シーンに配置

1. 上のプレハブを**自分以外の人数分**(最大2つ)シーンに置く。例: `Proxy_Slot0` / `Proxy_Slot1`
2. 空のGameObjectに `RemotePlayerProxyManager` をアタッチ(ワールドに常時1つ)
3. `proxyRigs` に `Proxy_Slot0`, `Proxy_Slot1` の `HumanoidProxyRig` を順番に入れる
4. `remotePlayerHeat` は熱の強さ(0〜1くらいで調整)

これで他プレイヤーの主要ボーン位置に毎フレーム自動で追従し、Echo役には輪郭だけ、Thermal役には熱だけが人型で見える状態になる。

これで毎フレーム他プレイヤーの腰の位置にプロキシが追従し、Echo役には球体の形だけ、Thermal役には熱表現だけが見える状態になる。
Memoryロールは元々Playerレイヤーを含めているので、他プレイヤーの実アバターがそのまま見える（プロキシは不要）。

---

## 2. CheckpointManager（セーブポイント）

空のGameObjectにアタッチ。

- `checkpoints[0]` = スタート地点のTransform
- `checkpoints[1]` = 赤ボタンの部屋
- `checkpoints[2]` = 青ボタンの部屋（レーザー）
- `checkpoints[3]` = 緑ボタンの部屋（最後の部屋）
- `doors` = 後述のToggleDoorを全部入れる

誰か1人が罠に触れると **全員** がここに戻る。

---

## 3. 赤・青・緑ボタン

**ShuffleSequenceManager**（空オブジェクト1個、マップに1つだけ）
- `shuffleManager` = ViewpointShuffleManager
- `checkpointManager` = 上のCheckpointManager
- `correctOrder` = 0, 1, 2（赤→青→緑）

**ShuffleButton**（ボタン3個それぞれに）
- `sequenceManager` = 上のShuffleSequenceManager
- `buttonId` = 赤:0 / 青:1 / 緑:2

押すと **視点シャッフル + セーブポイント登録 + 扉の開閉** が同時に起きる。

---

## 4. ToggleDoor（赤開く／赤閉じるドア）

扉ごとにアタッチ。

- `checkpointManager` = CheckpointManager
- `requiredCheckpoint` = 赤:1 / 青:2 / 緑:3
- `openWhenReached` = 「◯開くドア」なら **true** / 「◯閉じるドア」なら **false**
- `doorBody` = 閉じている時に置いておく板（Colliderつき）。開くとSetActive(false)される

状態は同期済みの `currentIndex` から計算するので、途中参加でもズレない。

---

## 5. QuizManager / QuizChoiceButton（クイズ部屋）

- `correctChoice` = 正解の選択肢番号（0始まり）
- `doorBody` = 正解で開く扉
- `trapFloor` = 不正解で消える床。**その下に落とし穴のHazardZoneを置く**
- `checkpointManager` = 床を使わず即セーブポイントに戻す場合のみ設定

選択肢ボタンごとに `QuizChoiceButton` を付けて `choiceIndex` を 0,1,2... と振る。
**問題文と選択肢のラベルはMemoryレイヤーに置く**（過去の人しか読めない）。

不正解後は再挑戦できる（ペナルティは落下と巻き戻しのみ）。完全に一回きりにすると詰むため。

---

## 6. PressurePlate / PressurePlateDoor（2つ踏むと開くドア）

**PressurePlate**（板ごと、IsTriggerのColliderが必要）
- `door` = 対応するPressurePlateDoor
- 板の見た目は **Echoレイヤー**（エコロケだけが形として認識できる）

**PressurePlateDoor**
- `plates` = 対応する板を全部
- `requiredCount` = 2
- `doorBody` = 扉の実体
- `stayOpen` = オフ推奨（降りたら閉まる。2人が踏み続けて1人が進む形になる）

同期変数を使っていないので設定漏れの心配はない。

---

## 7. HazardZone（落とし穴・レーザー）

- `checkpointManager` = CheckpointManager ← **これを設定すれば旧GameManagerは不要**
- `thermalWarning` = DynamicThermalObject（サーモ役への警告表示）
- レーザーのように点滅させるなら `activeDuration` / `inactiveDuration` を設定

---

## 未実装（今後）

- スプリンクラー（サーモとエコロケを同時に一時無効化）
- 偽ボタン（ランプの色にない色のボタン。過去の人だけ見分けられる）
- 天井ライトと落とし穴の対応づけ・チェス盤の床（配置作業）
