# BLIND 引き継ぎ書（AIエージェント向け）

最終更新: 2026-08-21 13:37

会話履歴なしで作業に入れることだけを目的に書いてある。
配置作業の手順は [`配置手順.md`](配置手順.md) にあるので、そちらと2枚で1組。

---

## 0. 最初に読む3行

1. **提出締切は 2026-08-21(金) 23:59。今日。** 残り約10時間。
2. **ワールドはまだ一度もVRChatにアップロードされていない**（`MainWorld` の blueprintId が空）。実機確認ゼロ。
3. **19部屋中18部屋にギミックが1つも無い。** ただし作者は既に案を持っており、「指示を出す」と明言済み。**勝手にギミックを提案しないこと**（一度やって「前話しただろ」と言われている）。

---

## 1. プロジェクト概要

**BLIND** — VRChat上の3人非対称協力ホラー探索ゲーム。IVRC2026 メタバース部門応募作品。

3人は**同じ部屋にいるのに見えているものが全く違う**。声で伝え合わないと進めない、というのが作品の核。

| 役割 | 見えるもの | 担当する情報 |
|---|---|---|
| **エコロケ** | 音波パルスが当たった面の**輪郭だけ**が0.6秒光る。それ以外は暗闇 | 空間の**形**（壁・段差・通路・什器） |
| **サーモ** | **熱源だけ**が色で見える。建物はほぼ真っ黒 | **危険と生き物**（炎・人・レーザー・配管・水） |
| **過去人** | 照明のある普通の世界。ただし「今そこにある物」の一部が見えない | **意味**（看板・文字・矢印・記憶） |

**設計の背骨**：サーモ役に部屋の「形」を読ませてはいけない。形を伝えるのはエコロケ役の仕事であり、そこが崩れると非対称協力が成立しない。温度表の `dim` 値はこの原則で決まっている。

世界観はリミナルスペース／バックルームズ系。**ここのクオリティには妥協しない**というのが作者の明言した方針。技術的都合で雰囲気を削る提案は歓迎されない。

### 審査スケジュール
- 作品投稿締切: **2026-08-21(金) 23:59** ← 今日
- 一次審査: 08-23(日) オンライン・**最大5分**・遠隔 → **ソロで成立する短縮版URLが別途必要**
- 二次審査: 08-29〜30 オンライン・最大20分・審査員が団体で入り制作者が同伴解説
- 現地展示・表彰式: 09-07〜08 @富山県立大学
- 提出物: **3つのURL**（完全版／一次審査用／二次審査用）＋ 作品解説文800字 ＋ **紹介動画 最大3分 mp4**

---

## 2. 環境

- Unity **2022.3.22f1** / **Built-in RP** / **Linear色空間**
- VRChat SDK3 (Worlds) + **UdonSharp** / ClientSim同梱
- リポジトリ: `/Users/ke-go/Documents/GitHub/IVRC-BLIND`（GitHub: tosou0802ksu-maker/IVRC-BLIND, `main`）
- メインシーン: **`Assets/_BLIND/Scenes/MainWorld/MainWorld.unity`** ← 作業対象はこれ一択
  - `soshi.unity` / `test3world.unity` は個人の作業場。触らない
- Unity操作は **unityMCP** の `execute_code` でエディタ拡張を直接叩ける（後述の実行例参照）

### 部屋の担当
- 作者担当: room7, 9, 11, 12, 13, 15, 16, 17, 19
- 他メンバー: room1, 2, 3, 4, 5, 6, 10, 14, 18（`MainWorld` にマージ済み）
- **他メンバーの部屋の見た目を変える改修は事前に確認を取ること。** レイヤー生成と軽量化は承認済み。

---

## 3. 最重要アーキテクチャ：「4つの世界」

**これを理解していない改修は100%壊れる。**

| Layer | 名前 | 誰に見えるか |
|---|---|---|
| 0 | Default | **過去人だけ** |
| 22 | Thermal | **サーモだけ** |
| 23 | Echo | **エコロケだけ** |
| 24 | Memory | 過去人だけ（看板・文字・「かつての壁」） |
| 25 | **NowOnly** | **誰にも見えない**（今日新設） |

各役割のカメラは自分のレイヤーだけを映し、背景は黒。
**同じ壁を3枚、レイヤー違いで重ねて置いている。**

```
Box_Root                     ← 位置と Collider はここに1つだけ
 ├ Vis_Default   (Layer 0)   ← 普通の見た目
 ├ Vis_Echo      (Layer 23)  ← EchoHighlight マテリアル
 └ Vis_Thermal   (Layer 22)  ← Thermal_*.mat
```

この重複を手で置くのは不可能（部屋19・レンダラー数千）なので、**Default層から Thermal層とEcho層を自動生成するツール**がある（§4）。

### Layer 25 `NowOnly` の使い方（今日新設）
「今そこにあるが、過去人には見えない物」を置くレイヤー。

- Default(0) の実体を 25 に移すと、`PlayerVisionController` が過去人のマスクから落とす
- Echo/Thermal の複製は今までどおり生成される（ビルダーは 25 を除外しない）
- **コライダーはそのまま残る** → 見えないのにぶつかる

現在の使用例：room14 のレーザー44個。エコロケにも過去人にも見えず、**サーモ役だけが警告できる**状態。

### Layer 24 `Memory` の裏技（今日追加）
過去人だけに見えるレイヤーなので、**実際には無い壁**を置ける。
`room9/Memory_SealedDoorway_East` が実例：東の開口(2m×3.25m)に壁を重ねてあり、
過去人は行き止まりに見えるが、コライダーが無いので通れる。エコロケ役は開口として見る。

### ⚠️ VRChat実機の落とし穴
シーンの `Camera.cullingMask` を書き換えても**実機では何も起きない**。
`VRC.SDK3.Rendering.VRCCameraSettings.ScreenCamera.CullingMask` を書き換える必要がある。
`PlayerVisionController.cs` は対応済み。`OnVRCCameraSettingsChanged` で再適用しないとVRChatにマスクを戻される。
**この経路は実機未確認。アップロード後に真っ先に確認すべき箇所。**

---

## 4. 自動生成ツール（`BLIND` メニュー）

すべて `Assets/_BLIND/Editor/`。**実行順序が決まっている。**

| メニュー | 実装 | 役割 |
|---|---|---|
| `BLIND/軽量化/1. メッシュを軽くする` | `BlindMeshReducer.cs` | 重いメッシュを頂点クラスタリングで減量 |
| `BLIND/軽量化/9. 軽量化を元に戻す` | 同上 | 元メッシュに差し戻す（元は消していないので常に戻せる） |
| `BLIND/部屋修正/1〜4` | `BlindRoomFixes.cs` | 部屋そのものの作り直し（§4.3） |
| `BLIND/vision/0. 実機テスト用の数値を一括適用` | `BlindTuning1200.cs` | エコロケ／サーモの数値を反映＋保存 |
| `BLIND/vision/1. サーマル材質を作り直す` | `BlindThermalTable.cs` | 温度別マテリアル生成 |
| `BLIND/vision/2. 全部屋にサーモ・エコロケを生成` | `BlindVisionBuilder.cs` | **本体** |
| `BLIND/vision/3. 選択した物を「現在だけ」にする` | `BlindNowOnlyTagger.cs` | 選択物を NowOnly(25) へ |
| `BLIND/vision/3b. 「現在だけ」を解除` | 同上 | Default(0) へ戻す |
| `BLIND/エコロケ受信機を集め直す` | `EchoReceiverCollector.cs` | `EchoReceiver` を `EchoEmitter` に登録 |

**正しい実行順**：
```
軽量化1 → 部屋修正(必要な分) → vision/1 → vision/2 → 受信機を集め直す → vision/0 → シーン保存
```

### 4.1 unityMCP からの実行例

メニューを `ExecuteMenuItem` で叩くと **モーダルダイアログでUnityが固まる**。必ずリフレクションで非ダイアログ版を呼ぶこと。

```csharp
// vision/2 を実行
var vb = System.Type.GetType("BLIND.EditorTools.BlindVisionBuilder, Assembly-CSharp-Editor");
var res = (string)vb.GetMethod("BuildMine").Invoke(null, null);

// 受信機の再登録（Collect はダイアログを出さない版）
var col = System.AppDomain.CurrentDomain.GetAssemblies()
    .SelectMany(a => { try { return a.GetTypes(); } catch { return new System.Type[0]; } })
    .FirstOrDefault(x => x.Name == "EchoReceiverCollector");
col.GetMethod("Collect").Invoke(null, null);

// 数値適用＋保存
var tu = System.Type.GetType("BLIND.EditorTools.BlindTuning1200, Assembly-CSharp-Editor");
tu.GetMethod("Apply").Invoke(null, null);
```

スクリプトを書き換えたら必ず：
```csharp
UnityEditor.AssetDatabase.ImportAsset("Assets/_BLIND/Editor/XXX.cs", UnityEditor.ImportAssetOptions.ForceUpdate);
UnityEditor.Compilation.CompilationPipeline.RequestScriptCompilation();
// 次のツール呼び出しの冒頭で必ず
if (UnityEditor.EditorApplication.isCompiling) return "compiling...";
```

### 4.2 `BlindVisionBuilder` の分類ルール（`Classify()`）

オブジェクト名・親名・マテリアル名を連結した文字列で判定。**上から順に評価されるので順序が意味を持つ**。

| 判定 | 結果 |
|---|---|
| `decal / stain / soot` | 除外（床とZファイティングするため） |
| 親をたどって `HotDolls` に一致 | `Body` 34℃（room16 の4体） |
| 親をたどって `Prop_GiantHand` | `Skin` 36℃ |
| `laser` かつ `glow` | 除外（発光板。画面を覆う） |
| `laser` | `Laser` 62℃ / **echo=false** |
| `lightpanel` | `Lamp` / `LampDim` / `LampOff` |
| `pooldeck / water` | `Water` 11℃（room7 の PoolDeck は名前に反して水面） |
| `wall / kabe / dado / baseboard / cornice / handrail` | `Wall` |
| `duct / pipe / vent / tube / valve / plumb / conduit / hanger` | `Duct` 38℃ |
| その他 | `Prop` 19℃ |

### 4.3 `BlindRoomFixes` の内容（今日作成・冪等）

| メニュー | 内容 |
|---|---|
| 1. room6とroom7の扉を繋ぐ | room7の扉を local z 5.70→3.50 に移動。開口を world z -17.50〜-16.30 にしてroom6と一致させる |
| 2. room14のレーザーをサーモ専用にする | レーザー44個を NowOnly(25) へ |
| 3. room9に「過去人だけ塞がって見える」通路を作る | 東の開口に Memory(24) の壁（コライダー無し） |
| 4. room16(人形部屋)を暗くする | 照明を元の35%に（**絶対値指定なので何度実行しても同じ**） |

---

## 5. サーモ視点の設計（`BlindThermalTable.cs`）

実際のサーモグラフィの値を基準にしつつ「順序と理由は現実どおり、差は読めるところまで開く」方針。
現実の室内は壁も床も17〜19℃に収まり、そのまま入れると画面が一色の紫になるため。

表示レンジ **12〜70℃**、ガンマ0.65。

| 帯 | 温度 | dim | 意図 |
|---|---|---|---|
| 床 15 / 壁 18 / 天井 21.5℃ | | 0.022〜0.034 | **ほぼ黒**。ここを上げるとサーモ役が自力で間取りを読めてしまい、背骨が折れる |
| 金属16 / 小物19 / 木20 / 布22℃ | | 0.075〜0.13 | 「そこに物がある」＝ぶつかる危険の予告。サーモの正当な領分 |
| 水 11℃ | | 0.24 | 落ちると危ないので例外的に読ませる |
| **配管 38℃** | | **0.55** | 今日引き上げた。長い線なので**間取りがある程度読めてしまう副作用がある** |
| 人体34 / 肌36 / CRT47 / 蛍光灯52 / **レーザー62** / 安定器68 / **燃える人66** / 炎95℃ | | 1.0 | サーモ役の道しるべ |

`dim` は距離フェードにも使われる（`_FadeNear` 5m 〜 `_FadeFar` 200m）。減光だけだと「近寄れば読める」ので距離でも塞いでいる。

### ⚠️ 表示レンジ上限70℃の意味
**70℃を超える温度は全部 h=1.0 に張り付いて同じ真っ白になる。**
レーザーを88℃にしていた時、炎95℃と区別が付かず、さらに画面が白飛びしていた。
**新しい熱源を足すときは必ず70℃以下に収めるか、`DisplayMax` ごと見直すこと。**

### ⚠️ Linear色空間の罠
`col *= _Dim` では体感の明るさが落ちない（0.05倍しても知覚は26%まで）。
シェーダーで `pow(_Dim, 2.2)` に補正済み。**表の数字と見え方がずれたらここを疑う。**

### ⚠️ チカチカ対策（再発させないこと）
1. ワールド空間ノイズが1ピクセルより細かいと激しくちらつく → `ddx/ddy` でピクセルのワールド幅を測りフェード
2. **時間で動くノイズを入れてはいけない。** VRだと左右の目に別のノイズが出て立体視が壊れる
3. VRChatのアンチエイリアスは**クライアント側設定**。プロジェクトのQualitySettingsはエディタにしか効かない

---

## 6. エコロケ視点の設計

### パルス（`EchoEmitter`）
| 項目 | 値 |
|---|---|
| 射程 | 12m |
| 角度 | 55度 |
| 自動間隔 | 2.2秒 |
| 点灯 | 0.6秒（`EchoReceiver.glowDuration`） |
| 距離遅延 | 0.045 秒/m |
| **手動パルス** | トリガー（デスクトップは左クリック）、クールダウン1.1秒 |

点灯総時間 = `距離×0.045×2 + 0.6`。12m地点で1.68秒、間隔2.2秒に対して**約0.5秒の暗闇**が残る。
**間隔を縮めるときは必ず `glowDuration` と `delayPerMeter` も同じ比率で縮めること。** 光が途切れなくなると、暗闇と一瞬の安心感というこの作品で一番効いている対比が消える。

手動パルスは `InputUse` で実装（VRChat標準、デスクトップ・VR両対応）。`allowManualPulse` で切れる。

### 材質は2種類（今日分離）
| マテリアル | 用途 | `_RimWeight` |
|---|---|---|
| `EchoMaterial` | 床・壁・天井・水（建物） | **0**（稜線のみ） |
| `EchoMaterial_Prop` | 什器・小物 | **0.45**（シルエット発光） |

**全部にリムを効かせてはいけない。** 床壁天井は必ずどこかが視線に対して縁になるので、画面全体が緑に埋まって暗闇が消える（実際にそうなった）。

### 未実装のオプション
`EchoEmitter.occlusionCheck`（壁の向こうを光らせない）は実装済みだが**既定オフ**。
コライダーの無い面が完全に光らなくなるリスクがあるため、実機で確認してから有効化すること。

---

## 7. 現在の進捗（数値）

```
三角形 / レンダラー数
  Default(0)   645,056 / 5,150
  Thermal(22)  162,317 /   218
  Echo(23)     251,791 /   380
  Memory(24)        12 /     1
  NowOnly(25)   15,356 /    44
  合計       1,074,532
EchoReceiver: 380個（UdonBehaviour 380 / 欠落 0）
シーン: GameObject 6,069 / コンフリクト 0 / 保存済み
```

ポリゴンは元の **7,497,586 → 1,074,532**（軽量化ツールによる）。

### 今日直したもの
- room14 レーザー：サーモ専用62℃の細い線に（白飛び解消）
- room16：4体の人形＋巨大な手に体温、照明35%、人形が人型シルエットで出るように
- room6↔room7：扉の位置ずれ2.2mを解消して接続
- room9：過去人だけ塞がって見える通路を設置
- 配管92本＋空調＋通気口に熱
- room4：什器のシルエットが出るように（材質分離）
- エコロケ：射程22→12m、間隔3.5→2.2秒、手動パルス追加

---

## 8. 現状の問題

### 未着手
| 項目 | 状況 |
|---|---|
| **VRChatへのアップロード** | 未実施。blueprintId 未設定。**最優先** |
| **実機での3役視界確認** | 未実施 |
| アヒルの角度による輪郭欠け（room7） | 原因把握済み。丸い物は箱投影UVの「端」が無い。`EchoMaterial_Prop` のリムで解決できるはず。作者は「最悪無視でいい」と言っている |
| ギミック配置 | **作者が案を持っている。指示待ち。勝手に提案しないこと** |
| NowOnly の部屋ごと選定 | 作者の方針は「部屋ごとに手で選ぶ」。候補を出して承認をもらう流れ |
| 過去人用の看板・矢印（Memory層） | 作者が「文字・看板・矢印」＋「かつての間取りの痕跡」を選択済み。room9に1件実装しただけ |
| 一次審査用ソロ短縮版 | 何も決まっていない |
| 作品解説文800字 / 紹介動画3分 | 未着手 |

### 注意が必要な既知の副作用
- **配管を38℃/dim0.55にした結果、サーモ役が天井の線から間取りをある程度読める。** 元は意図的に暗くしていた箇所。効きすぎるようなら `BlindThermalTable.cs` の `Duct` 行を戻す
- room6 の `Grass v2` が5枚ほぼ同じ場所に重ねて置かれている。4枚消せば無料でさらに削れる（他メンバーの部屋なので未実施）
- 草は軽量化で緑のマットになっている。`BLIND/軽量化/9.` で戻せる
- 重い部屋: room16 / room12 / room9

---

## 9. ⚠️ 事故と再発防止（必読）

### 9.1 シーンがマージコンフリクトで壊れた（今日発生）
コミット `9b0579d`（10:56「Update MainWorld.unity」）に **`git stash` 由来のコンフリクトマーカーが152箇所**入ったままpushされていた。Unityはこれを読めず、**シーンを中身ゼロで開く**。

- Unityのログに `The file '...' seems to have merge conflicts.` と出る
- **この状態で保存すると32MBのワールドが空で上書きされる**
- 復旧: `git show 1746c37:Assets/_BLIND/Scenes/MainWorld/MainWorld.unity > 同パス`
- 壊れた版は `scratchpad/MainWorld.BROKEN-9b0579d.unity` に退避済み

**作業前に必ず確認：**
```bash
grep -c '^<<<<<<<' Assets/_BLIND/Scenes/MainWorld/MainWorld.unity   # 0 であること
```

**保存する処理には必ずガードを入れること。** `BlindTuning1200.Apply()` は Renderer が100個未満なら中止する。

### 9.2 UdonSharp のコンポーネント二重構造
`AddComponent<T>()` は **C#プロキシしか作らない**。エディタ上は正常に見えるのに、**アップロードすると完全に動かない**。

```csharp
UdonSharpEditor.UdonSharpUndo.AddComponent(go, typeof(EchoReceiver));
// 値を書いた後
UdonSharpEditor.UdonSharpEditorUtility.CopyProxyToUdon(usb);
```

検証は必ず UdonBehaviour 側の値を読むこと：
```csharp
var ub = UdonSharpEditor.UdonSharpEditorUtility.GetBackingUdonBehaviour(proxy);
ub.publicVariables.TryGetVariableValue("glowDuration", out object v);
```

### 9.3 `LayerMask` へのビット演算がUdonで落ちる
`mask &= ~(1 << 25)` は `int → LayerMask` の暗黙変換を呼ぶが、**この演算子はUdonに公開されていない**。
`Method is not exposed to Udon` でU#のコンパイルが丸ごと落ち、**その結果シーンが空で読み込まれる**（原因が非常に分かりにくい）。
`LayerMask → int` の向きだけは公開されているので、**最初に int にして以降 int で通すこと**。

### 9.4 `EditorUtility.DisplayDialog` は自動化から呼ぶな
モーダルなのでUnityが完全にフリーズする。ダイアログ版とロジック版を必ず分けること。

### 9.5 「今の値に係数を掛ける」処理は冪等にすること
`l.intensity *= 0.35f` を二度実行して room16 が0.12倍の真っ暗になった。
**元の値を表に持って絶対値で入れる**書き方にする。

### 9.6 簡易形状（箱）の落とし穴
バウンディングボックスで代用するので、
- **斜めに走る細い物**（レーザー・パイプ）は実体の何十倍もの板になる → `IsThinAndLong()` で除外
- **巨大な物**（8mのアヒル）は部屋を塞ぐ → `IsBig()` で除外
- **人体**を箱にすると「人の形」という情報が消える → `Body/Skin/Burning` は300三角形に粗くしてでも実形状

### 9.7 検証は目視で済ませない
これまでのバグは全て「エディタ上は正常に見えた」もの。
一時カメラでレイヤー別スクリーンショットを撮り、中心ピクセルを数値で比較する。
オブジェクトを1つずつ無効化して輝度の変化を見る二分探索が有効（`t_room14` の白飛び特定はこれで解決した）。

**注意**：エコロケの検証で全受信機の `_GlowIntensity` を1にすると**マップ全体が光る**ので、遠くの部屋まで写って誤診する。実機では12m以内しか光らない。

---

## 10. ファイル地図

```
Assets/_BLIND/
├ 引き継ぎ書.md            ← このファイル（現状把握）
├ 配置手順.md              ← ギミック配置の手順書（作業時はこちら）
├ CREDITS.md               ← 使用アセットのクレジット（未完）
├ Scenes/MainWorld/MainWorld.unity   ← ★作業対象
├ Editor/
│  ├ BlindVisionBuilder.cs   ★ Thermal/Echo層の自動生成（本体）
│  ├ BlindThermalTable.cs    ★ 温度・減光の設計表
│  ├ BlindMeshReducer.cs     ★ 頂点クラスタリング減量
│  ├ BlindRoomFixes.cs       ★ 部屋そのものの修正（冪等）
│  ├ BlindNowOnlyTagger.cs   ★ NowOnly(25) への移動
│  ├ BlindTuning1200.cs      ★ 数値一括適用＋保存ガード
│  ├ EchoReceiverCollector.cs
│  └ Room12*.cs / Room16*.cs  部屋ごとの生成ツール
├ Scripts/                 ← ランタイム（UdonSharp）
│  ├ PlayerVisionController.cs  ★ 役割別カリング（VRCCameraSettings）
│  ├ EchoEmitter.cs / EchoReceiver.cs  ★ エコロケのパルス
│  ├ ThermalSurface.shader      ★ サーモの見た目
│  ├ EchoHighlight.shader       ★ エコロケの輪郭
│  ├ LampPuzzleManager.cs 他    ギミック（コードは完成済み・未配置）
│  └ HazardZone.cs / CheckpointManager.cs ほか
└ Art/
   ├ Materials/Thermal/    温度別マテリアル（自動生成）
   ├ Materials/EchoMaterial.mat        建物用（リム0）
   ├ Materials/Echo/EchoMaterial_Prop.mat  什器用（リム0.45）
   └ Models/Lite/          軽量化された複製
```

---

## 11. 作者とのやり取りで守ること

- **ギミックの案を勝手に提案しない。** 作者は案を持っており、指示を出すと明言している
- 不明点は**逆質問で潰す**方針を作者自身が指示している。曖昧なまま大規模改修に入らない
- **作者はAPIトークンをチャットに貼ったことがある。** キーやトークンの入力・記述はAI側では行わず、必要なら作者本人に依頼する
- 報告は「何をしたか」だけでなく**なぜその値・その手法なのか**を書く。コードのコメントも同じ方針
- 他メンバーの部屋（room1,2,3,4,5,6,10,14,18）の見た目を変える改修は事前確認

---

## 12. 今日の残りでやるべきこと（優先順）

1. **`MainWorld` をVRChatにアップロード**（未実施・最優先）
2. **実機で3役の視界を確認**。`VRCCameraSettings` が効いていなければ全員に通常の世界が見える（すぐ分かる）
3. **エコロケが実機で光るか**（UdonBehaviour修正後の実機確認は未実施）
4. 作者からのギミック指示を受けて実装
5. 3つのURL用意 → 作品解説文800字 → 紹介動画3分

**1〜3が終わるまで新機能に着手しない。** 実機で視界が出ないなら、機能をいくら足しても意味がない。

**未コミット変更が289ファイルある。復旧点として今すぐコミットすることを強く勧める。**

---

## 13. チームメンバーからの引き継ぎ指示（残タスク）

以下のギミックのアタッチと設定が残っている。次のエージェントはこれを最優先で手動（またはUdonSharpUndo経由のスクリプト）で設定すること。

### 落とし穴と普通の穴への付与
- **普通の穴**: 床に当たり判定をなくして、エコロケは与えない。
- **落とし穴**: 床に当たり判定をなくして、エコロケを与えて、サーモを渡す。
※現状の `BlindGimmickBuilder` の穴生成ロジックがこれと食い違っている場合は、この指示を優先してビルダーを修正するか手作業で直すこと。

### クイズとドア
ドアを全部閉めた状態にしてから、ゲームマネージャーとドアとリスポン地点に付与する。問題文と選択肢の貼り付け。
- **DoorQuizChoice**（ドア1枚ごとに付ける）
  - プレイヤーがInteractしたら、自分の番号を `DoorQuizManager` に送る。
  - `doorQuizManager` — このドアが属する `DoorQuizManager` をドラッグ
  - `doorIndex` — このドアの番号（0, 1, 2）
- **DoorQuizManager**（1問につき1つ配置）
  - 選択されたドア番号と正解を比較し、正解→ドアを移動、不正解→全員チェックポイントへ戻す。
  - `correctDoorIndex` — 正解のドア番号（0, 1, 2）
  - `door0` / `door1` / `door2` — 3枚のドアのTransformをドラッグ
  - `door0OpenPosition` / `door1OpenPosition` / `door2OpenPosition` — ドアの開いた位置のVector3（正解ドアのみ実際に使用される）
  - `checkpointManager` — シーン内の `CheckpointManager` をドラッグ
  - `correctSound` / `wrongSound` — 正解時・不正解時の音（任意）

### 各種ドアとボタン（対応するドアとボタンにコンポーネント付与）
- **MultiButtonDoor**
  - GameObjectにアタッチする。複数のボタンがインタラクトされてand回路でドアを開閉する。
  - 移動させるドアとドアの開閉時の座標を設定できる。開閉時の効果音も設定できる。
- **MultiButtonDoorButton**
  - ボタンにアタッチする。ボタンIDで重複を避ける。ボタン押した時の効果音を設定できる。
- **SingleButtonDoor**
  - ボタン１とドア１（マネージャー側）
- **SingleButtonDoorButton**
  - ボタン１とドア１（ボタン側）
- **SingleButtonDualDoor**
  - ボタン１とドア２（マネージャー側）
- **SingleButtonDualDoorButton**
  - ボタン１とドア１（ボタン側）

### 感圧板とドア（対応するドアと感圧板にコンポーネント付与）
- **DualPressurePlateDoor**
  - GameObjectにアタッチする。2枚の感圧板の状態を見てドアを開閉させる。
  - 移動させるドアとドアの開閉時の座標を設定できる。開閉時の効果音も設定できる。
- **DualPressurePlate**
  - 既存の感圧板オブジェクトにアタッチする。乗り降りを検知してマネージャに伝える。

### CheckpointManager（シーンに1つ）
不正解時などに `TriggerDeath()` が呼ばれ、全員を現在のセーブポイントにテレポートさせる。
- `checkpoints` — Transform配列。[0] はスタート地点（復帰先、空のGameObject）。[1] 以降は各セーブポイント。
- `doors` — 進行で開閉するToggleDoor（なければ空）
- `deathEffect` / `deathSound` — 死亡演出・音（任意）
